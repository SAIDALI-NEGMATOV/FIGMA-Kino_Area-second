import { createTheme } from "@mui/material";

const theme = createTheme({
  breakpoints: {
    values: {
      Phone: 0,
      Planshet: 768,
      Pluse:1000,
      Notebook: 1024,
      Plu:1325,
      NoteL: 1400,
    },
  },
});

export default theme;
